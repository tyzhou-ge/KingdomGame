# 游戏设计文档：KingdomGame (v2.0)

## 1. 游戏概念

`KingdomGame` 是一款多智能体回合制策略游戏。在一个 10x10 的网格地图上，5个由不同智能体（AI或人类）控制的国家，通过资源积累、领土扩张和军事征服进行博弈。游戏的核心乐趣在于观察一个**可被直观可视化的**、**动态演变的沙盘过程**，体验领土和实力变化的策略深度。

## 2. 核心游戏机制 (v2.0)

### 2.1. 游戏目标
- **胜利条件**：成为地图上最后一个幸存的国家。
- **失败条件**：本国的“首都”格子被敌方占领。一旦首都失陷，该玩家立即被淘汰，其剩余领土和兵力变为中立（或由其他规则处理），不再主动行动。

### 2.2. 地图与领土
- 地图为一个 10x10 的网格。
- 每个格子在任意时刻都归属于一个玩家，或是中立。
- 每个玩家拥有一个位置固定的“首都”格子。

### 2.3. 回合与决策
- **同步回合制**。
- **指令继承**：当一个新回合开始时，每个人类玩家的所有格子将**默认执行上一回合的指令**。人类玩家只需修改那些需要改变策略的格子。

### 2.4. 资源：兵力 (Armies)
- **产出**：每个属于玩家的领土格子，在每个回合开始时，会产出1个“0岁”的新兵。
- **信息不对称（战争迷雾）**：玩家只能看到自己领土上每个格子的兵力数量和构成。对于敌方或中立领土，玩家只能看到其归属，无法看到其兵力部署。所有玩家的首都位置是公开信息。

### 2.5. 核心机制：寿命与后勤压力
- **兵力寿命 (Lifespan)**：每个兵都有固定的寿命。如果一个兵在寿命结束前没有在战斗中消耗掉，它将因“衰老”而自动消亡。
- **堆叠惩罚/后勤压力 (Stacking Penalty)**：为了防止玩家无限制地屯兵形成“死亡军团”，当一个格子上聚集的兵力超过某个“舒适容量”时，该格子上所有兵的衰老速度会不成比例地加快。这鼓励玩家将兵力分散部署，形成战线，而不是聚于一点。

### 2.6. 行军与战斗
- **行军**：(无变化)
- **战斗**：(无变化)
- **大规模战斗**：当一场战斗的总参与兵力（双方合计）超过一个阈值（`LARGE_BATTLE_THRESHOLD`）时，该格子的边界将在下一回合**以红色高亮**，作为“上古战场”的标记。

## 3. 玩家与智能体 (v2.0)

- 游戏支持多种类型的智能体：
  - **`RandomAgent`**: 随机移动。
  - **`GreedyAgent`**: 优先攻击能打得过的最弱的敌人。
  - **`DefensiveAgent`**: 优先巩固边防和首都。
  - **`StrategicAgent` (新增)**: 一个更高级的AI，能够根据全局态势在“和平扩张”、“边境战争”和“斩首行动”等多种宏观策略间切换。
  - **`HumanAgent`**:
    - **操控模式**：支持两种模式，可在 `config.py` 中配置。
      1.  **`RECURSIVE` (递归遍历模式)**: 系统按固定顺序依次高亮玩家的每个领土，等待玩家输入指令。
      2.  **`FREE_ROAM` (自由漫游模式)**: 玩家从首都开始，使用方向键自由地在自己的领土上移动高亮框，为选中的格子下达指令。随时可以按 `Enter` 结束回合。
    - **孤岛地块**：在 `FREE_ROAM` 模式下，那些从首都无法通过连续的己方领土到达的“孤岛”地块，如果玩家没有手动选择它们，它们将执行**随机**移动。

## 4. 视觉与交互 (v2.0)

- **平台**：使用 Python 的 Pygame 库进行2D图形渲染。
- **配色**：(无变化)
- **界面与视觉反馈**：
  - **颜色深度**：格子的颜色会根据其内部兵力的**平均年龄**动态变化。年龄越大，颜色越深，直观反映出这是一支“老兵”还是“新兵”。
  - **指令箭头**：当人类玩家为某个格子确定了行军方向后，该格子上会**立即显示一个箭头**，清晰地标示出已设定的指令。
  - **战斗高亮**：发生过大规模战斗的格子，其边框会变为红色。
  - **人类交互**：
    - **`RECURSIVE` 模式**: `w,a,s,d,space` 控制方向，`q` 撤销。
    - **`FREE_ROAM` 模式**: `w,a,s,d,space` 控制方向，**箭头键**移动高亮框，`Enter` 确认结束回合。

## 5. 网络功能 (v2.0)

- **架构**: 游戏将支持基于 `websockets` 的客户端/服务器（C/S）架构，实现局域网联机对战。
- **同步机制**:
  - **服务器 (Server)**: 作为游戏的主持者。它运行核心的 `GameEngine`，负责接收所有玩家的指令，结算回合，然后将最新的完整游戏状态 (`GameState`) 广播给所有客户端。
  - **客户端 (Client)**: 每个玩家运行一个客户端。客户端负责渲染从服务器接收到的 `GameState`，并为本地玩家（人类或AI）提供决策界面/逻辑。决策完成后，客户端仅将**指令**发送回服务器。
- **一致性**: 所有游戏逻辑和随机性计算**只在服务器端**发生，确保了所有玩家看到的游戏世界是完全一致的。

