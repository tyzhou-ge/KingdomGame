# 游戏数据结构与算法 (v2.0)

本文档详细定义了 `KingdomGame` 的核心数据结构、常量、算法和公式，是实现游戏引擎的基础。

## 1. 游戏常量 (Config)

这些参数应定义在统一的配置文件中，便于调整和平衡。

- `MAP_WIDTH`, `MAP_HEIGHT`: 10, 10 (地图尺寸)
- `NUM_PLAYERS`: 5 (玩家数量)
- `BASE_LIFESPAN`: 5 (兵力的基础寿命，单位：回合)
- `SUPPLY_LIMIT`: 10 (单个格子的后勤容量/舒适容量)
- `CAPITAL_MIN_DISTANCE`: 8 (首都之间需要保持的最小曼哈顿距离)
- **`LARGE_BATTLE_THRESHOLD`**: 10 (定义“大规模战斗”的兵力总和阈值)
- **`HUMAN_INPUT_MODE`**: `'RECURSIVE'` 或 `'FREE_ROAM'` (人类玩家的操控模式)

## 2. 核心数据结构 (Models)

### `Tile` (格子)
- **`owner`**: `Player` 对象或 `None`。表示当前格子的归属。
- **`armies`**: `list[int]`。一个队列（列表），`armies[i]` 代表年龄为 `i` 的兵的数量。例如 `[10, 5, 3, 0, 0]` 表示有10个0岁兵，5个1岁兵，3个2岁兵。列表长度等于 `BASE_LIFESPAN`。
- **`is_capital`**: `bool`。标记此格是否为首都。
- **`last_turn_battle_size`**: `int`。记录上一回合在此格子发生的战斗规模（双方总兵力），用于判断是否标记为大规模战斗。默认为0。

### `Player` (玩家)
- **`id`**: `int`。唯一标识符。
- **`name`**: `str`。
- **`color`**: `tuple[int, int, int]`。RGB颜色值。
- **`capital_pos`**: `tuple[int, int]`。首都的 (x, y) 坐标。
- **`is_defeated`**: `bool`。标记玩家是否被淘汰。
- **`agent`**: `BaseAgent` 对象。控制该玩家的智能体。
- **`last_actions`**: `dict[str, str]`。存储该玩家上一回合提交的行军指令，用于新回合的默认指令。

### `GameState` (游戏状态)
- **`map`**: `list[list[Tile]]`。一个二维列表，构成游戏地图。
- **`players`**: `list[Player]`。游戏中的所有玩家对象。
- **`current_turn`**: `int`。当前的回合数。

## 3. 核心算法与公式

### 3.1. 首都生成算法
1.  对于第一个玩家，随机在地图上选择一个点 `(x, y)` 作为其首都。
2.  对于后续每个玩家：
    a. 随机生成一个候选首都点 `(nx, ny)`。
    b. 计算该点与所有已存在的首都之间的**曼哈顿距离** (`abs(nx-ex) + abs(ny-ey)`)。
    c. 如果**任何一个**已计算的距离小于等于 `CAPITAL_MIN_DISTANCE`，则判定该候选点不合法，返回步骤 a 重新生成。
    d. 如果所有距离都大于 `CAPITAL_MIN_DISTANCE`，则该点合法，设为当前玩家的首都。

### 3.2. 兵力衰老与后勤压力算法 (Attrition)
此过程在每回合的**末尾**结算。
1.  遍历地图上的每一个格子 `tile`。
2.  **重置战斗标记**：将 `tile.last_turn_battle_size` 设为 0。
3.  计算该格子的总兵力 `N = sum(tile.armies)`。
4.  如果 `N > SUPPLY_LIMIT`，计算衰老速度：`aging_speed = ceil(1 + (N - SUPPLY_LIMIT) / SUPPLY_LIMIT)`。
5.  否则，`aging_speed = 1`。
6.  **更新兵力年龄**：创建一个新的空兵力队列 `new_armies`。遍历旧队列 `tile.armies` 中年龄 `i` 的兵，数量为 `count`。他们的新年龄将是 `i + aging_speed`。将 `count` 个兵累加到 `new_armies` 中对应新年龄的位置（向下取整）。
7.  最后，移除所有年龄大于等于 `BASE_LIFESPAN` 的兵。
8.  在回合开始时，新产出的兵总是加入到年龄为0的位置：`tile.armies[0] += 1`。

### 3.3. 战斗公式 (Combat)
当多个国家的兵力在同一回合进入同一个格子时，触发战斗。
1.  **战斗前结算**：首先结算所有移动，确定每个冲突格子的最终攻击方和防守方。如果一个格子有多个攻击方，他们将作为一个联合力量。
2.  **记录战斗规模**：在计算 `A_effective` 和 `D_effective` 之前，将双方总兵力 `total_force = A + D` 记录到 `dest_tile.last_turn_battle_size`。
3.  **计算有效兵力**：
    - 攻击方总兵力 `A` = 所有攻击方在该格子的兵力之和。
    - 防守方总兵力 `D` = 防守方在该格子的兵力。
    - `A_effective = ceil(A * random(0.7, 1.0))`
    - `D_effective = ceil(D * random(0.5, 1.0))`
    (`ceil` 为向上取整)
4.  **判定胜负**：
    - 如果 `A_effective > D_effective`，攻击方胜利。
    - 否则，防守方胜利。
5.  **计算损失**：
    - **若攻击方胜**：
        - 攻击方损失 `D_effective` 的兵力（按比例从各个攻击方部队中扣除）。
        - 防守方全灭。
        - 格子归属变更为攻击方（如果是多国联攻，则随机选择一个攻击方或按贡献度分配）。
    - **若防守方胜**：
        - 防守方损失 `A_effective` 的兵力。
        - 攻击方全灭。




### 3.4. 视觉相关算法
- **格子颜色深度算法**：
  1.  对于一个格子 `tile`，如果其总兵力 `N > 0`，计算其兵力的加权平均年龄：`avg_age = sum(age * count for age, count in enumerate(tile.armies)) / N`。
  2.  计算一个颜色调整因子 `factor = 1 - (avg_age / BASE_LIFESPAN) * 0.6`。这个因子范围大致在 `[0.4, 1]` 之间，年龄越大，因子越小。
  3.  将 `tile.owner.color` 的R, G, B分量分别乘以 `factor`，得到最终显示的颜色。`final_color = (r*factor, g*factor, b*factor)`。


## 4. 智能体接口 (Agent Interface)

### `BaseAgent` (抽象基类)
(无变化)

### `HumanAgent` (人类玩家)
- **操控模式一 (`RECURSIVE`)**:
  - 游戏按固定顺序（如从上到下，从左到右）遍历玩家所有领土，依次高亮。
  - 对于每个高亮的格子，等待玩家的键盘输入（`w`, `a`, `s`, `d`, `space`）。
  - 收到输入后，记录指令，并高亮下一个格子。
  - 支持按 `q` 键撤销上一步操作。
- **操控模式二 (`FREE_ROAM`)**:
  - 游戏开始时高亮玩家的**首都**。
  - 玩家使用 `w, a, s, d, space` 为当前高亮的格子设置行军方向。
  - 玩家使用**方向箭头键**（Up, Down, Left, Right）来移动高亮框到相邻的己方领土。如果目标格子不属于自己，移动失败，高亮框不动。
  - 玩家随时可以按 `Enter` 键结束本回合的决策。
- **指令继承与默认值**：
  - 在决策开始时，所有格子的默认方向继承自 `player.last_actions`。
  - 对于 `FREE_ROAM` 模式，当玩家按下 `Enter` 后，所有未被手动指定的格子将使用其默认（上一回合）的指令。
  - **孤岛地块处理**：对于那些从首都无法通过己方领土到达的“孤岛”地块，如果玩家在 `FREE_ROAM` 模式下没有手动为它们指定方向，它们将执行**随机**移动。

### `StrategicAgent` (高级策略智能体)
这是一个新的AI类别，其决策逻辑如下：
1.  **全局扫描**：检查自己的所有领土格子是否有任何一个与敌国领土相邻。
    - **情况A：没有敌人相邻（和平扩张）**：
      - 遍历所有领土，找到那些与中立（无人占领）格子相邻的。
      - 指挥这些前线格子向最近的中立格子扩张。
      - 内部的格子则向最近的前线格子集结兵力。
    - **情况B：有敌人相邻（战争状态）**：
      - **首都圈防御检查**：
        - 遍历所有与敌国相邻的己方格子 `my_tile`。
        - 检查 `my_tile` 是否在首都的“绝对防御圈”内（例如，与首都的曼哈顿距离 `≤ 2`）。
        - 检查 `my_tile` 是否位于某个敌方格子到己方首都的最短路径上。
        - 如果同时满足以上条件，则 `my_tile` 的指令设为 `stay`，以执行防御任务。
      - **常规进攻**：对于其他与敌国相邻的格子，指挥它们进攻相邻的敌方格子。
      - **首都圈安全评估**：
        - 检查首都周围半径为3的区域是否**全部**为己方领土。
        - 如果是，则触发“斩首行动”：找到离自己最近的敌方首都，指挥所有兵力沿着最短路径向该首都进军。
        - 如果否，则继续执行常规的边境攻防。

## 5. 网络同步协议 (Networking)

- **架构**: C/S (Client-Server)。
- **技术**: `websockets` 库。
- **流程**:
  1.  **启动**: 一名玩家运行 `server.py` 开启游戏服务器。所有玩家（包括服务器玩家）运行 `client.py`。
  2.  **连接**: 客户端连接到服务器的IP和端口。服务器等待直到 `NUM_PLAYERS` 个客户端全部连接。
  3.  **游戏开始**: 服务器初始化 `GameEngine`，并将完整的初始 `GameState` 序列化（如用`pickle`或`json`）后广播给所有客户端。
  4.  **回合同步**:
      a. **决策阶段**: 每个客户端的 `HumanAgent` 或 `AIAgent` 根据本地收到的 `GameState` 做出决策，并将指令字典发送给服务器。
      b. **等待阶段**: 服务器等待接收所有存活玩家的指令。
      c. **结算阶段**: 服务器收齐所有指令后，在自己的 `GameEngine` 实例上执行一个完整的游戏回合逻辑 (`run_turn`)。
      d. **广播阶段**: 服务器将更新后的 `GameState` 再次序列化并广播给所有客户端。
      e. 客户端收到新的 `GameState` 后，更新本地显示，进入下一回合的决策。
- **数据**：网络间只传输 `GameState` 和指令字典。所有游戏逻辑和随机事件的计算**仅在服务器端**进行，保证所有客户端的一致性。
